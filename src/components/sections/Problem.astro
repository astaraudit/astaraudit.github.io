---
---

<section id="problem" class="section problem" aria-labelledby="problem-heading">
  <div class="container problem__grid">
    <header class="problem__head">
      <span class="section-label">§ 01 · The problem</span>
      <h2 id="problem-heading">
        Most audits check code against a list.
        <span class="problem__heading-tail">
          We check code against its <em>own logic</em>.
        </span>
      </h2>
    </header>

    <div class="problem__body">
      <p class="dropcap">
        Pattern-matching audits compare your code to a library of known bugs<a class="fn" href="#fn-1" id="fnref-1" aria-describedby="fn-1">¹</a>.
        If the vulnerability fits a known pattern, they catch it. If it doesn't,
        they miss it. Novel bugs, composition-layer vulnerabilities, and edge
        cases at mathematical boundaries all fall through.
      </p>

      <aside class="sidenote" role="note">
        <span class="sidenote__num">Note 1</span>
        Slither, Mythril, and the long tail of detection rules. Useful, but
        bounded by what they were taught to look for.
      </aside>

      <p>
        DeFi protocols are built on mathematical invariants — properties that
        must hold true across every state transition. A rounding error in a
        lending protocol's interest calculation or an edge case in an AMM's
        bonding curve math can drain millions. These bugs don't match patterns.
        They <em>violate invariants</em>.
      </p>

      <p class="problem__footnote" id="fn-1" role="doc-footnote">
        <span class="problem__footnote-num">1.</span>
        Static analyzers, signature-based scanners, and rule-driven linters.
        <a href="#fnref-1" aria-label="Back to text">↩</a>
      </p>
    </div>
  </div>
</section>

<style>
  .problem__grid {
    display: grid;
    grid-template-columns: minmax(0, 5fr) minmax(0, 7fr);
    gap: clamp(2rem, 6vw, 5rem);
    align-items: start;
    position: relative;
  }

  .problem__head h2 {
    margin-top: 0.4rem;
  }

  .problem__heading-tail {
    display: block;
    color: var(--color-ink-2);
    font-style: italic;
    font-weight: 400;
  }

  .problem__heading-tail em {
    color: var(--color-accent);
    font-style: italic;
  }

  .problem__body p {
    color: var(--color-ink-2);
    font-size: 1.08rem;
    line-height: 1.72;
    text-wrap: pretty;
    max-width: 60ch;
  }

  .problem__body p + p { margin-top: 1.3em; }
  .problem__body em { color: var(--color-ink); font-style: italic; }

  .problem__footnote {
    margin-top: 2.6rem !important;
    padding-top: 1rem;
    border-top: 1px solid var(--color-rule);
    font-family: var(--font-mono);
    font-size: 0.78rem !important;
    line-height: 1.6 !important;
    color: var(--color-ink-3);
    max-width: 60ch;
  }

  .problem__footnote-num {
    color: var(--color-accent);
    margin-right: 0.5em;
    letter-spacing: 0.04em;
  }

  .problem__footnote a {
    color: var(--color-ink-3);
    margin-left: 0.4em;
  }

  @media (max-width: 880px) {
    .problem__grid { grid-template-columns: 1fr; gap: 1.6rem; }
  }
</style>
